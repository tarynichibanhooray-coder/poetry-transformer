# Example environment variables for Poetry Transformer
# Copy to .env and replace values before running locally.

# OpenAI API key (required for translation calls)
OPENAI_API_KEY="sk-REPLACE_WITH_YOUR_KEY"
OPENAI_MODEL="gpt-4o"

# Optional: if using pi_trigger.py to call a remote server
SERVER_URL="http://localhost:8000"
